# Dépendances pour le générateur d'images Lelins
# Installer avec : pip install -r scripts/requirements.txt
#
# Note PyTorch :
# - macOS Apple Silicon : la version par défaut inclut MPS, rien de spécial à faire.
# - Windows NVIDIA : utiliser l'index CUDA approprié, voir docs/INSTALL.md.
# - Linux NVIDIA : idem, index CUDA.
#
# Notes téléchargements automatiques (au premier run) :
# - SDXL base                    : ~7 Go  (cache HuggingFace)
# - SDXL inpainting (tryon.py)   : ~7 Go  (cache HuggingFace)
# - IP-Adapter face (face lock)  : ~2 Go  (h94/IP-Adapter + image encoder)
# - IP-Adapter général (tryon)   : ~600 Mo (réutilise l'image encoder)

torch>=2.2.0
torchvision>=0.17.0

diffusers>=0.29.0
transformers>=4.40.0
accelerate>=0.29.0
safetensors>=0.4.0
huggingface-hub>=0.22.0

# Traitement image
Pillow>=10.0.0

# Divers
tqdm>=4.66.0

# Serveur web (scripts/serve.py)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
jinja2>=3.1.0
